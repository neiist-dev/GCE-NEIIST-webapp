import { FilterResultsPipe } from './filter-results.pipe';

describe('FilterResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
